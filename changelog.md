# Level 0 Engine Changelog

## [v1.2.2] - 2026-08-18

_Papering A Crawlspace, And What The Old Paper Was Hiding_

### Added

- **[ASSETS] A Real Paisley Generator (`SurfaceTextures._buildPaisleyWallpaper`):** The first pass drew its boteh as a hand-tuned chain of eight bezier segments, which is why it read as a symmetrical blob rather than a motif. The outline is now generated: a spine walks forward while its heading accelerates from straight into a curl (`angle = bend * t^1.5`), carrying a half-width profile that peaks close to the base (`sin(PI * t^0.6)` — the fractional exponent is what pulls the maximum down to t≈0.31). That combination is what produces a round belly and a long hooked tip. Layout moved from a 2x2 grid to **two serpentine stems**, the second phase-shifted half a period for a half-drop, each stem a single sine over the full canvas height so its endpoints share an x and it rejoins itself across the seam. Motifs hang off the stem's extremes rather than sitting on a grid, which is the difference between reading as wallpaper and reading as tiled stamps. Bodies are two-tone with a beaded inner border, leaves are angled to the stem tangent, and the ground carries roll striae and foxing. **Seamless by construction** — every mark is emitted through a `tile()` helper that repeats it at the nine neighbouring canvas origins, so anything crossing an edge arrives on the far side. Verified against a 2x2 with the seams drawn in, and a 4x4 to confirm the repeat does not announce itself.
- **[ASSETS] Duct Interior Is Now Four Materials, Not One (`SurfaceTextures._buildDuctInteriorSet`):** `ductWallMat` (paisley), `ductFloorMat` / `ductCeilingMat` (worn butt-jointed boards, one material shared by both), and `ductTornMat` (see below). Not only an art split — the lining's vertical and horizontal faces cannot share a UV scale (see Fixed), and a repeat lives on a texture, which lives on a material. All four run through `makeDuctInterior`, so the whole tube stays AO-killed. World tile is 0.8m on every face; one UV unit is 4m on every axis except wall V, which `buildWall` maps to 3m, so only that repeat differs — `(5, 3.75)` for walls against `(5, 5)` for the panels.
- **[ASSETS] Torn Wallpaper Edge (`SurfaceTextures._buildDuctTornEdge`):** A cutout strip standing 2mm proud of each wall panel along the bottom 12cm — plaster below an irregular tear, a hairline of paper backing along the cut, fully transparent above so the paisley shows through. Cut with `alphaTest` rather than blended, so there is no transparency sorting to get wrong. Two things make it hold: the tear profile is a low flat baseline plus four narrow gaussian lifts evaluated across the wrap (`k = -1, 0, 1`) so they cross the seam intact, with only integer-frequency jitter carrying the ragged fibre; and the strip tiles at **1.6m against the wallpaper's 0.8m**, so its period outlasts the panels it sits on and no wall shows the same tear twice. The first attempt used a single sine the width of the tile outlined in a 3-7px pale cream ribbon, which produced a continuous rolling silhouette that read as fungus and repeated visibly — recorded here because the failure was entirely in the low frequency and the bright continuous lip, not in the idea.
- **[WORLD] Guaranteed Far Exit On Duct Runs (`CrawlspaceDuct`):** A post-pass after pruning measures the Manhattan distance from the start cell to the farthest cell carrying an exit. Under 3 cells, it collects every network face bordering a genuinely openable neighbour — same `isWall` / `isAirlockApron` / `isLowClearance` tests growth uses, so it cannot open a door into solid — and promotes the farthest. At most one exit added, and only to networks that are currently clustered.

### Fixed

- **[WORLD] Duct Floor And Ceiling Sampled A 1/75th Sliver Of Their Texture (`CrawlspaceDuct.buildDuctLining`):** `buildWall` gives side faces world-proportional UVs against a 4m x 3m reference, but scales a slab's top and bottom V by `h / 3.0`. The duct floor and ceiling linings are 0.04m thick, so their visible faces sampled **V ∈ [0, 0.0133]** — one seventy-fifth of the texture stretched across the entire panel. Every horizontal surface in the duct was a smear of a single texture row, and no texture would have survived it. Those faces now build through a duct-local helper that puts world-proportional UVs on ±Y against the same 4m reference the walls use. Contained to the duct rather than changing `buildWall`, which every thin horizontal slab in the game depends on.
- **[WORLD] Z-Fighting At Every Duct Junction (`CrawlspaceDuct.buildDuctLining`):** `buildWall` inflates every box by 0.02 on all three axes to hide seams between maze walls. Duct lining is not maze wall — its pieces are laid out to abut exactly, a hub's 1.2m footprint meeting a 1.4m branch at `holeW/2` — so that inflation turned every junction into a band of two coplanar surfaces: hub-to-branch floor and ceiling overlapping 2cm with tops both at `ductY+0.05` and `ductY+1.15`, and corner-to-side lining overlapping in a 2cm column with inner faces both at `cx-0.55`. It had always been there and was invisible only because the old smeared UVs made both sides of each fight identical. All **11 lining pieces** now build at exact nominal size and meet edge to edge with nothing overlapping.
- **[WORLD] Dead Ends Rendered As Bare Yellow Maze Wall (`CrawlspaceDuct`):** `capLining` seals the hub face where a branch would have gone, but was positioned off the dead branch cell rather than the hub's own lining plane. Measured from hub centre: corner posts present their inner face at `0.56`, the `block` behind is a `buildWall` and so grows to `0.59`, and the cap sat at `0.60` — strictly behind the block, which therefore occluded it on every pixel. With everything still inflated the cap had landed at `0.59`, exactly coplanar with the block face, which was itself a good share of the reported fighting. Now placed at `holeW/2 - liningT/2 = 0.58`, level with the posts and `0.03` clear of the block, with width `holeW - 2*liningT = 1.12` so it spans *between* the posts and abuts rather than overlaps.
- **[WORLD] Duct Runs Had Both Doors At The Entrance (`CrawlspaceDuct`):** `initialExits` was doing double duty — deciding whether the duct had any exit at all, *and* spending `maxExits`. Every open side of the starting cell became a door and incremented `numExits`, so a duct punched into a wall between two corridors began with the whole budget (2 or 3) already gone, and `numExits < maxExits` then refused every exit growth tried to place along the run. A 15-tile network could end with both its doors side by side at the mouth: crawl the length of it and the only way out is where you came in. The existing `totalRemainingExits < 2` guard never caught it because it counts exits without caring where they are. The start cell now claims **one** entrance chosen at random from its open sides, the budget is raised to `3 + random()*2`, and growth spends the remaining 2-3 out along the run. The four-way openable test, previously written out three times in three slightly different shapes, is now a single hoisted predicate.

### Changed

- **[ASSETS] Duct Ceiling Uses The Floor Boards:** In a crawlspace the ceiling is the underside of the floor above, so papering it was the odd choice out. Both keys point at one material rather than two clones, so they share a merge group instead of splitting into two draw batches; the differing ±Y box face winding keeps the ceiling from reading as an exact mirror of the floor underfoot.

### Known

- **[ASSETS] `rustMat`'s override is correct and should stay.** Flagged in v1.2.1 as an open art decision; investigated and closed. The pipeline's version (`HazardTextures.js:99`) is `{color: 0x3a1c14, roughness: 1.0, metalness: 0.3}` — a bare flat colour with no map and no bump — while the `MaterialLibrary` version that has been silently winning carries `corrosionBumpTexture`. Swapping would have downgraded every rusted surface in the game. It stays declared in `ASSET_OVERRIDES`.
- **[ASSETS] The `makeDuctDoorMat` concern from v1.2.1 is withdrawn.** It was raised without checking. `doorMat` is a genuine six-material array (`StructuralTextures.js:97`), the `isX` branch swaps the large faces onto ±X before selecting, and the duct-facing face is darkened correctly in both orientations. Only the four 10cm edges stay bright, which is defensible since they sit in the wall aperture.
- **[WORLD] The duct door casing must not be run through `makeDuctInterior`.** Tried, and it blacked out the jamb as seen from the lit corridor — the trim sits at the cell face and is visible from both sides, and a black AO map cannot serve both. It is deliberately left at full ambient with a comment at the call site. The correct fix, if the interior glare ever matters, is separate interior and exterior trim pieces, not one material trying to be both.
- **[ASSETS] `_buildPaisleyWallpaper` still accepts `faded` and `stained`, and nothing calls them.** Roughly 30 lines — the washed palette and the water-stain blooms — left reachable only by argument. Kept because they are parameters of a reusable generator rather than dead branches in a hot path, and they are exactly what a papered ceiling would want back.
- **[WORLD] The torn strip's phase steps at panel boundaries.** Its period is world-fixed at 1.6m while adjacent panels differ in length (`lDepth` 1.4 against a cap's 1.12), so the tear does not flow continuously around a corner. It reads as separate strips of paper, which is arguably correct, but it is a consequence rather than a decision.
- **[WORLD] Duct frequency may fall slightly.** A network that previously qualified on two entrance-cell doors now needs growth or the far-exit backstop to find a genuine second exit, or it is rejected by the `< 2` guard. That is the intended outcome — a duct whose only doors are side by side is not worth generating — but `MIN_EXIT_SPREAD` and `maxExits` are the dials if ducts start feeling scarce, or if 3-4 doors per network reads as too permeable.

## [v1.2.1] - 2026-08-17

_The Wallpaper Was Never Missing, And Neither Was The Save_

### Fixed

- **[ASSETS] The Paisley Duct Lining Was Built Correctly And Then Thrown Away (`MaterialLibrary.js`):** A new procedural paisley wallpaper was wired into `ProceduralTextureFactory.generateAssets` as `ductWallMat` and never appeared in engine. The generator was never at fault. `Environment.setup()` does `Object.assign(this, assets)` at line 81, but `MaterialLibrary.injectMaterials` runs later — from `generate()` at line 497 — and did `env.ductWallMat = makeDuctInterior(env.sharedWallMat.clone())` unconditionally, replacing the paisley material with a duct-darkened clone of the ordinary wall texture before a single duct was built. Every `env.ductWallMat` reference in `CrawlspaceDuct.js` was therefore reading the overwrite. The local build is now a fallback (`if (!env.ductWallMat)`) for boot paths that supply nothing. Verified off the running engine: `ductWallMat` is the 512x512 canvas material at `repeat 2,2`, sRGB, `roughness 0.9`, `bumpScale 0.02`, `aoMapIntensity 0.98`, `userData.ductInterior === true`; **40 meshes reference it**, and a camera raycast inside a crawlspace duct hits a lining face carrying the paisley map at 1.91m.
- **[SYSTEM] Autosave Was Silently Not Running (`SaveManager.idleSaveState`):** Reported as "the save didn't put me back where I left off", which looked like a restore bug and was not one. `startAutoSave` fires a 2500ms interval into `idleSaveState`, which queued every write through `requestIdleCallback` **with no `timeout` option**. A continuously rendering WebGL loop can saturate the frame budget indefinitely, and the browser then never reports an idle period, so the callback simply never runs — no error, no warning, no write. Measured live with the page reporting `visibilityState: 'visible'` and `hasFocus: true`: **`requestIdleCallback` fired 0 times in 3000ms while a control `setTimeout` fired normally.** The stored blob consequently kept whatever had been written during an early moment of boot when idle time still existed, which is exactly the "returned to the spawn point" symptom. Now `{timeout: 1000}`, which is what actually guarantees the callback runs. The failure is non-deterministic by nature — an earlier probe in the same session *did* persist within 3505ms — so it presents as an intermittent save bug rather than a scheduling one. Persistence and restore themselves were verified sound both before and after: a distinctive position round-trips through reload and CONTINUE exactly.

### Added

- **[SYSTEM] Asset-Clobber Detector And Override Registry (`MaterialLibrary.ASSET_OVERRIDES`, `_snapshotAssets`, `_reportClobberedAssets`):** The `ductWallMat` bug is a class, not an incident — anything `injectMaterials` assigns unconditionally silently wins over the asset bundle, because the bundle always lands first. All 36 keys `injectMaterials` writes were cross-referenced against everything the pipeline produces (`assets/textures/metadata.json` for the static path, the generator return objects for the fallback), and **exactly two collide: `ductWallMat` and `rustMat`.** `injectMaterials` now snapshots every material and texture on `env` at entry, diffs by identity at exit, and warns for any replaced key not declared in `ASSET_OVERRIDES`. Scoping the snapshot to materials and textures is deliberate: it keeps mutation-in-place (the `serverFloorMat.map.repeat` calls) and non-asset resets like `env.observers` — which `generate()` legitimately re-initialises at line 457, before this runs — from generating noise. Verified silent on a clean boot and correctly loud against a simulated clobber, with the declared `rustMat` override suppressed.
- **[SYSTEM] Per-Boot Save Backup And Recovery (`SaveManager.recoverBackup`):** `loadState()` now snapshots the previous session's raw blob into `level0_state_backup` once per boot, before the current session's autosave can touch it, and `recoverBackup()` promotes it back to the live slot and reseeds the IndexedDB copy. A boot that fails to restore and then overwrites is now recoverable rather than terminal — the failure mode that destroyed the evidence while diagnosing the entry above. The blob measured **861 bytes**, so the second slot costs nothing.

### Changed

- **[SYSTEM] Autosave Refuses Unsafe Writes (`SaveManager._refuseSaveReason`):** `saveState` previously guarded only on `player.isDead` and otherwise wrote unconditionally every 2.5s. It now returns a reason string and refuses on four conditions: the player is dead; boot has not finished restoring (`bootComplete`, armed by `main.js` immediately before `startAutoSave`); spawn placement is in progress; or `bestDepth` would regress below the stored value, which is the signature of a boot that failed to restore. The spawn check reads `isSpawning || needsSafeSpawn` rather than `isSpawning` alone, because `ChunkManager` clears `isSpawning` at line 218 *immediately before* it relocates the player, leaving a window where the camera still sits on the raw reset position. Refusals log once per distinct reason rather than every tick. The load-bearing case is a mid-session `generate()` — what a warp or inquest regeneration does, with autosave live — and it was measured directly: `generate()` parked the camera at the chunk-origin reset position `-826` with `isSpawning` true, the save **held at `-777` for the whole spawning window**, and only resumed once the player had been safe-spawned to a valid cell at `-822`. **The raw reset position never reached disk.** The predicate itself returns the correct reason for all four refusal branches and `null` when healthy.

### Known

- **[ASSETS] `rustMat` is overwritten by `injectMaterials` on every boot, and always has been.** It ships in `metadata.json`, is also built by `HazardTextures`, and `MaterialLibrary` replaces it unconditionally with a material built from `corrosionBumpTexture`. Every rusted surface in the game therefore renders with the `injectMaterials` version and the pipeline's copy is dead weight. It was **deliberately not guarded**: doing so would swap in a material the game has never rendered with and change the appearance of every rusted surface, which is an art decision rather than a bug fix. It is declared in `ASSET_OVERRIDES` instead, which preserves current behaviour exactly and makes the precedence explicit. If the pipeline's version is the intended one, that is a separate, deliberate change.
- **[SYSTEM] The `bestDepth` guard blocks deliberate rollbacks.** Autosave will not write a state whose `bestDepth` is lower than the stored blob's, so restoring an older save by letting the engine overwrite is no longer possible — use `recoverBackup()` or edit `localStorage` directly. A New Game is unaffected, since its purge clears the store first.
- **[SYSTEM] The original lost-position event could not be reconstructed.** By the time the save blob was inspected it already held the spawn coordinates, and the mechanism that produced them destroys its own evidence. The starved `requestIdleCallback` explains the symptom completely and is independently demonstrated, but it is an inference about that specific event rather than a replay of it. The backup slot exists so the next occurrence leaves something to read.

## [v1.2.0] - 2026-08-17

_Plugging The Leaks In The Render Pipeline_

### Changed

- **[GRAPHICS] Shadow Map Resolution And Filtering (`RenderEngine.js`):** The engine was previously hardcoded to use `THREE.PCFSoftShadowMap` regardless of the quality setting. With 6 active shadow-casting point lights, this forced the GPU to take 120 shadow texture samples per fragment, destroying fill-rate performance on even high-end hardware (e.g., 45fps on a 7800XT at 1080p). The default 'high' setting now correctly maps to the much cheaper `THREE.PCFShadowMap` (4 samples per light), and `PCFSoftShadowMap` has been moved to a new 'ultra' quality tier.
- **[GRAPHICS] Drastically Reduced Fragment Shader Lighting Loops (`LumenGrid.js`):** `maxActiveLights` was reduced from 32 to 16. In a forward renderer, Three.js compiles standard materials with distance and attenuation loops sized to the maximum number of lights in the scene, meaning every pixel on screen was running math for 64 total lights (32 point + 32 spot) even if their intensities were zero. Halving the active pool cuts the shader overhead by 50%. As an added architectural benefit, reducing the pool means the engine is no longer forced to assign slots to "bare" (non-shadow-casting) lights from distant rooms, which inadvertently fixed a massive light-bleed issue where specular highlights from adjacent sectors were shining straight through solid walls.
- **[PERFORMANCE] Bypassed Expensive Scene Graph Recalculations (`InteractionController.js`):** The interaction hot loop previously called `.getWorldPosition()` on hundreds of interactive objects and doors every frame. This implicitly triggered `updateWorldMatrix(true, false)` in Three.js, forcing a recursive check up the entire scene hierarchy. Replaced with a direct read (`setFromMatrixPosition(obj.matrixWorld)`), as the renderer's pre-render matrix update ensures this data is already perfectly fresh.
- **[GRAPHICS] Disabled Logarithmic Depth Buffer By Default (`RenderEngine.js`):** `logarithmicDepthBuffer` breaks early-Z hardware culling on many GPUs. Since the camera frustum sits comfortably between `0.1` and `100`, standard 24-bit depth precision handles it flawlessly. Disabled by default, recovering significant fill-rate overhead.

### Fixed

- **[PERFORMANCE] Shadow Prewarm Queue Now Properly Drains (`ChunkManager.js`):** The asynchronous `drainShadowPrewarm` loop used during chunk generation was hard-capped to 12 passes (`~96ms`). If a chunk contained more materials than could be compiled within that window, the queue aborted. When those remaining materials eventually fell into a shadow map frustum during gameplay (e.g., on first movement or light switch), Three.js was forced to synchronously compile the missing shaders on the main thread, causing severe frame drops. The pass limit has been raised to 150, guaranteeing the queue finishes entirely in the background before the chunk goes live.

## [v1.1.9] - 2026-08-17

_Measuring The Chunk Builder, And Discovering It Was Never Building_

### Added

- **[TOOLING] Per-Phase Chunk Build Profiler (`ChunkManager._profBegin/_profAdd/_profEnd`):** v1.1.8 closed with two named suspects for the chunk phase — the unyielded quadratic breach-wall pass, and the `setTimeout(0)` slice budget — and a note saying to instrument before touching either. Instrumenting first was correct, because **both were wrong.** Every chunk build now records `worker` / `cells` / `breach` / `instances` / `shadow` / `warm` / `yielded` into `env.genStats.phases`, with a full copy of the single worst chunk retained in `genStats.worstProfile`. `yielded` is tracked separately from work so "this chunk is expensive" can be told apart from "this chunk yielded a lot and the scheduler was slow returning". Roughly ten `performance.now()` calls per chunk, so it stays on permanently. The first run settled the question immediately: of 3902ms across 9 chunks, **breach was 12ms (0.3%), cells 84ms, worker 16ms, instances 23ms, yielded 5ms — and shadow prewarm was 3735ms (96%)**. Both v1.1.8 hypotheses are formally withdrawn; neither was worth optimising.

### Changed

- **[GRAPHICS] Shader Warmup Compiles The Variants Materials Actually Use (`warmMaterialVariants`, `queueShadowPrewarm`, `_unwarmedMaterials`):** With the profiler pointing at `drainShadowPrewarm`, the question became why it cost so much. `plain`, `instanced` and `instanced + instanceColor` are three separate entries in both the visible and depth program cache keys, so the warmup compiled three programs per material — blind, because `ShaderWarmup` runs before any chunk exists and cannot know how a material will be used. Surveying a live scene showed how badly that guess paid: **328 distinct materials produced 984 compiled shadow variants of which only 352 were ever used — 64% waste — and no material was used in all three forms, while 304 of 328 were used in exactly one.** The split is lopsided rather than even: 317 materials appear as `plain`, 28 as `coloured`, 7 as `instanced`. Warming is now form-aware end to end. `_unwarmedMaterials` reads the real form off each object (`isInstancedMesh`, `instanceColor`) and returns a `Map<Material, Set<form>>`; `queueShadowPrewarm` stores `{material, form}` pairs; the probe rig lights only the probe matching the requested form; and the warm-state key includes the form, so warming a material as `plain` can never mask a later, genuinely-needed `coloured` warm — the failure mode that would have reintroduced a cold compile on first sight. `ShaderWarmup` blind-warms `plain` only, and defers the two instanced forms to the chunks that actually create them, where `_compileInstances` has already decided the answer and the drain still runs before `contentReady` is set. Measured end to end: **shadow prewarm 3735ms → 1805ms, chunk phase 3678ms → 2000ms, engine boot 6131ms → 4607ms (25% faster)**. `_programKeepAlive` also stops growing three clones per material, falling from 1043 entries to 659 after an identical boot.
- **[UI] Boot Phase Bands Re-Measured:** With the chunk phase down from 3678ms to 2000ms it no longer dominates — it now sits roughly level with the program link (1947ms). `BOOT_PHASES` re-weighted accordingly: CHUNKS 16-76% → 15-58%, SHADERS 76-99% → 58-99%, and the smaller phases nudged to match a 4607ms baseline.

### Fixed

- **[PERFORMANCE] Chunk Streaming's Blocking Shader Link, Verified:** v1.1.8 shipped this fix flagged as "verified structurally, not by frame timing" — the mechanism was read and corrected but never demonstrated. It has now been measured directly, by counting entries into the unbudgeted `_drainProgramLinks` path (the one that skips `KHR_parallel_shader_compile`'s completion check) across a fixed 12-chunk-step traversal building 36 fresh chunks. **With the fix: 0 blocking drains, 898 budgeted ones. With the pre-fix behaviour restored on the same build: 55 blocking drains.** The dangerous path is deterministically gone. Frame timing across the same traversal came out at worst frame 121ms, 2 frames over 100ms, 17 over 30ms, median 17ms — against 231ms / 2 / 24 / 19ms before the form-aware warming above, so streaming is no worse and probably better. See Known for why the frame numbers alone still cannot carry the claim.

### Known

- **[PERFORMANCE] Batching the shadow probe rig was tried and reverted.** Compiling 8 materials per `renderer.render()` instead of one moved total shadow prewarm only 3735ms → 3547ms, inside run-to-run noise. That is itself a useful result: it establishes the cost is the driver compiling each depth program (~4.7ms per program, ~750 programs) rather than per-render overhead worth amortising, which is what redirected the work toward compiling *fewer* programs instead of compiling them faster. It was reverted rather than kept because it also made the per-frame drain in `animate()` strictly worse — a batch cannot be interrupted, so its 2ms budget would only be checked after eight materials rather than one. The rig keeps its multi-slot structure with `SHADOW_PROBE_BATCH = 1`, and the constant carries the measurement.
- **[PERFORMANCE] The remaining 1805ms of shadow prewarm is real compile work, not waste.** With the 64% variant waste removed, what is left is roughly 350 depth programs that genuinely get used. It is paid behind the loading screen rather than during play, which is the correct place for it, and there is no further redundancy identified in it. Making it smaller now means compiling fewer materials, not compiling them more cleverly.
- **[PERFORMANCE] Traversal frame times still cannot be A/B'd cleanly in one session, exactly as v1.1.3 warned.** Running the pre-fix arm second produced *better* frames (worst 70ms) than the fixed arm running first (worst 231ms) — not because the old code was faster, but because the browser's persistent GPU program cache was warm by then and the blocking drains found everything already linked. Every frame-time figure in this entry is directionally useful and none of it is a controlled comparison. The blocking-drain *count* is the load-bearing evidence here precisely because it is immune to that confound. A cold-cache-per-trial harness remains unbuilt.
- **[PERFORMANCE] Chunk queue ordering, predictive prefetch and worker pipelining are all closed as not worth doing.** Each was a v1.1.8 candidate; the profiler priced them at 16ms (worker), 84ms (cells) and 23ms (instances) across nine chunks combined. There is no meaningful time in chunk *generation* — the phase was never building geometry, it was compiling shaders.
- **[SYSTEM] `_programKeepAlive` still grows without bound**, just three times slower. Form-aware warming means one clone per material-form rather than three per material, but nothing releases them, and `_forgetMaterialPrograms` still clears the warm-state keys on chunk disposal so a re-created identical material re-warms and appends again. Unchanged in kind from v1.1.8.

## [v1.1.8] - 2026-08-17

_Boot Told The Truth, And The Truth Was Somewhere Else_

### Fixed

- **[ASSETS] The Static Texture Pipeline Was Silently Corrupting Two Materials:** Found while extending the exporter to cover per-sector bundles, not while looking for it. `assets/export_textures.html` recorded a fixed list of scalar properties per material and had no notion of material *kind*, so `StaticTextureLoader` rebuilt everything it loaded as a `MeshStandardMaterial`. Two assets did not survive that round trip. `flareMat` is a `THREE.SpriteMaterial` — `ImpoundSector.js:151` does `new THREE.Sprite(env.flareMat.clone())`, and a `Sprite` cannot render with a mesh material at all, so every impound flare had been drawing with the wrong material class since the static pipeline landed. `glowMat` lost `blending: THREE.AdditiveBlending` and its `polygonOffset`/`polygonOffsetFactor` pair, because the metadata schema never had fields for them — every additive glow in the static set was coming back as ordinary alpha blending with its depth offset gone. Neither failed loudly; both simply looked slightly wrong forever. The schema now carries `blending`, `polygonOffset`, `polygonOffsetFactor`, `side`, `depthWrite`, `alphaTest`, `vertexColors`, plain-data `userData`, and an explicit material kind (`Material` / `BasicMaterial` / `SpriteMaterial`), and the loader reconstructs each accordingly. Verified off the running engine after a re-export: `flareMat.isSpriteMaterial === true` with `blending === 2` (`THREE.AdditiveBlending`) and its map attached, `glowMat.blending === 2` with `polygonOffset === true`. Metadata and disk now agree exactly — 169 expected, 169 present, zero orphans, zero missing.
- **[PERFORMANCE] Chunk Streaming Blocked On Shader Links Mid-Build:** `ChunkManager.warmChunkMaterials` runs at every yield point inside a chunk build — roughly every 5ms of generation — and reached `warmMaterialVariants` and `_scopedCompile` on their old `drainNow = true` defaults. That path calls `_drainProgramLinks()` with no budget, and the polling guard is written as `polling = ext !== null && budgetMs !== Infinity`, so the unbudgeted call **skips the `KHR_parallel_shader_compile` completion check entirely** and calls `getUniforms()` on every pending program regardless of whether the driver has finished linking it. The v1.1.3 work removed exactly this stall from the boot warmup but left it in place on the per-chunk path, where it fired far more often. It only ever bit on first traversal into fresh geometry, because `_warmedMaterials` suppresses the warm on every subsequent visit — which is precisely the "mild stuttering when first traversing" shape reported. `warmChunkMaterials` now passes `drainNow: false`, and the default on both `warmMaterialVariants` and `_scopedCompile` was flipped to `false` so a blocking drain is a deliberate opt-in rather than something a caller inherits by omission. The per-frame `drainProgramLinks(1.5)` in `animate()` collects the results through the polling path that was always there for it.
- **[UI] Boot Phase Counter Ran Backwards:** `Environment.setup()` advanced the sequence to phase 4, then `main.js` re-announced phase 3 immediately afterward. The badge displayed `PHASE 03/06` after phase 4 had already run, and `phaseDurations` accumulated a duplicate, meaningless phase-3 row that landed in the end-of-boot `console.table`. The stale call is gone. Verified by sampling the live overlay every 100ms across a full boot: **zero backward steps**, monotonic to 100%.
- **[UI] In-Game Sector Loads Displayed A Frozen 100% Bar:** Every sector transition called `setPhase(3, 'LOADING ANOMALOUS SECTOR...', 50)` on the already-finished boot controller. That could not work by construction — `targetProgress` only ever moves through `Math.max`, so it was still pinned at 100 from the end of boot, and `_onFrame`'s `requestAnimationFrame` loop had already exited on completion, so nothing was animating anyway. Players saw a stale `PHASE 03/06` badge over a full bar reading `[ 100% ]` on every transition. Replaced with a real state teardown (`beginSubLoad` / `endSubLoad`) rather than a reuse of the boot phase machinery: progress resets, the rAF loop restarts, the badge reads `SECTOR LOAD` instead of a fabricated phase number, the minimum-duration gate is bypassed, and a synthetic crawl carries the bar because the chunk builder genuinely cannot report meaningful progress mid-transition. `endSubLoad` stops the loop again so the overlay is not left spinning for the rest of the session.
- **[TOOLING] Reported Boot Time Excluded Most Of Boot:** `BootController.startTime` is set inside `init()`, which runs after the browser has parsed the HTML, fetched and evaluated `r160.js`, and walked the entire 122-module ES graph — and after the Continue prompt has been answered. `[BOOT] Complete in Xms` therefore quoted engine-phase time as if it were total boot, understating it, while folding it all together would instead blame the engine for however long a player sat deliberating at the Continue screen. `main.js` now captures a mark before the prompt and another after it, and `finish()` prints a three-way `console.table` — document + modules, Continue prompt (user-gated), engine phases — plus the total. Measured on the reference machine: **182ms pre-engine, 4740ms at the prompt, 6131ms of engine boot**. The prompt time is real and was previously invisible; it is now impossible to mistake for engine cost.

### Changed

- **[SYSTEM] HTTP Caching Replaces `Cache-Control: no-store` (`engine_server.js`):** The dev server sent `no-store` on every response, so each boot re-downloaded `r160.js` (1.3MB), all 122 ES modules (1.3MB) and the entire texture set — which is almost certainly why the IndexedDB texture cache exists at all: it was working around a header the server itself was setting. Two lanes now. Content versioned by its own filename and never edited in place (`r160.js`, `assets/fonts/`) gets `public, max-age=31536000, immutable`. Everything else — source modules, HTML, and the texture WebPs, which *are* rewritten whenever the exporter re-runs — gets `no-cache` plus a weak `ETag` built from size and mtime, so the browser keeps the body but revalidates and receives a bodiless 304 when nothing changed. `immutable` was deliberately not used for `assets/textures/`: a stale WebP served mid-export-iteration would be a miserable bug to chase, and revalidation captures nearly all of the win without that risk. Measured on a warm reload: `r160.js` **0 bytes transferred** against 1,316,151 decoded, `vt323-latin.woff2` **0 bytes**, and a representative module (`Environment.js`) **300 bytes** against 30,965 decoded. The success branch also had to be restored — an in-flight edit to remove the `no-store` header had deleted the entire `else` arm, leaving every successful request with no response written at all. `PORT` now honours `process.env.PORT`.
- **[NARRATIVE] Case Data Loads In Parallel:** `StoryEngine.loadData` awaited seven `fetch` calls inside a `for` loop — seven sequential round trips for 128KB. Now a single `Promise.all`; per-file progress still reports as each individually lands, just in completion order rather than declaration order. An `res.ok` check was added, since failures were previously swallowed into a generic catch and reported as nothing at all.
- **[ASSETS] Texture Loading: One IndexedDB Transaction, Off-Thread Decode, No Leaked Object URLs:** `StaticTextureLoader` called `TextureCache.getBlob` per texture, and each call opened its own readonly transaction — roughly 95 separate open/commit cycles during the busiest stretch of boot. Added `TextureCache.getAllBlobs()` (`getAllKeys` + `getAll` on one transaction) and `saveBlobs()` for the write side, and the loader now primes once and resolves everything against that map. Decoding moved from `THREE.TextureLoader` — which goes through an `<img>` element and decodes on the main thread — to `createImageBitmap`, which decodes off it; this also removes object URLs from the path entirely, so the ~95 `URL.createObjectURL` calls that were never revoked no longer exist to leak. `imageOrientation: 'flipY'` paired with `texture.flipY = false` reproduces the old upload orientation exactly, and that was verified rather than assumed: sampling 5,476 points of `wallTexture` against a raw decode gives a **100% mirror match and 16% identity match**, confirming the browser honours the orientation hint. `THREE.TextureLoader` remains as an automatic fallback, now with proper revocation.
- **[ASSETS] Six Of Eight Sector Texture Bundles Are Now Static:** `lazyLoadSectorAssets` synthesised all eight per-sector bundles on the CPU every boot even though `USE_STATIC_TEXTURES` was already true for the 57-entry core set. Each bundle is now tried against the exported static set first and only generated on a miss. The exporter classifies every asset it encounters and records anything it cannot represent as a `residual`; a sector with a non-empty residual list is skipped wholesale rather than partially reconstructed, because half-static/half-generated would leave cross-referenced assets dangling. Result: **ANNEX, IMPOUND, BOARDROOM, ATRIUM, CHECKPOINT and INCINERATOR load statically; MAINTENANCE and ARCHIVE stay on the generator** (see Known). Bundle time fell **218ms → 120ms**, and the win landed where it mattered — INCINERATOR **101ms → 26ms**, CHECKPOINT **44ms → 17ms** — while the two sectors that stayed procedural were already 1-2ms each. `ProceduralTextureFactory.sectorGenerators()` is now the single source of truth shared by the runtime fallback and the exporter, so adding a sector reaches both.
- **[TOOLING] Exporter Emits WebP Directly:** The exporter wrote PNGs and the loader fetched `.webp`, with an undocumented manual conversion step in between that no longer had to exist. It now calls `canvas.toDataURL('image/webp', 0.92)` and the server derives the file extension from the data URL's declared MIME type, so output format is chosen in exactly one place. Export endpoints are origin-relative rather than hardcoded to `localhost:8080`. Full set: **169 files, 5.1MB** (was 95 files / 3.5MB — the growth is the newly-covered sector bundles).
- **[UI] Boot Progress Reweighted From Measurement, Not Intuition (`BOOT_PHASES`):** The old milestones (15/40/65/70/85/98) were hardcoded across three files and bore no relation to cost — the two cheapest steps owned 40% of the bar. Percentage bands now live in one exported table in `BootController`, and every call site reports a 0..1 fraction of its own step via `setPhaseProgress`; no other file hardcodes a boot percentage. Bands are sized from a measured 6131ms boot: DATA 365ms (0-6%), ASSETS 177ms (6-9%), GRID 2ms (9-10%), BLUEPRNT 355ms (10-16%), **CHUNKS 3678ms (16-76%)**, SHADERS 1553ms (76-99%), STABLE 1ms (99-100%). Reweighting is now a one-line edit against the console table the controller already prints.
- **[UI] Boot Shader Compile Sliced, With A Crawl Underneath:** `renderer.compileAsync(scene, camera)` over the whole scene measured **1075ms** and reports nothing while it runs, so the bar parked at its ceiling for over a second — the worst "is it frozen?" moment in the sequence. It now compiles a few top-level groups at a time by swapping `scene.children` (lights and cameras stay resident in every slice, since materials must compile against the lighting they will actually render with), with an asymptotic synthetic crawl covering each individually-opaque slice. Slice count matters far more than expected and was measured, not guessed: **1 slice → 1075ms, 3 slices → 938ms, 27 slices → 4853ms.** A few coarse slices cost nothing and give the bar real checkpoints; fine slicing is badly superlinear. `COMPILE_SLICES = 3`.
- **[UI] Boot Overlay Stopped Forcing Layout Every Frame:** `updateDOM` runs on every animation frame and unconditionally rewrote the log box via `innerHTML` — an HTML parse — then read `scrollHeight`, forcing a synchronous reflow, roughly sixty times a second for the whole of boot, competing with the very work it was reporting on. Element lookups are now cached, each field is guarded by a change check, and the log box is rebuilt from DOM nodes only when its contents actually change.

### Added

- **[UI] Self-Hosted VT323 (`assets/fonts/`):** `engine.html` loaded its only typeface from `fonts.googleapis.com` via a render-blocking stylesheet, which meant the boot loading screen itself could not paint until a third-party host answered — a hard dependency on external network availability before the engine could show that it was doing anything. Both latin subsets are now served from this origin (34KB total, SIL Open Font License 1.1), with the `@font-face` rules inlined directly into `engine.html` rather than linked, so the boot-critical path has zero render-blocking stylesheet round trips of any kind, and a `preload` hint starts the woff2 fetch before CSS-to-glyph matching would. `splash.html`, `index.html` and `readme.html` share `assets/fonts/vt323.css`; those three still reach Google for **Space Mono**, which is unchanged. Unicode ranges are copied verbatim from the Google Fonts CSS API so glyph coverage and per-range loading behave identically. Verified live: `document.fonts.check('16px VT323') === true`.
- **[SYSTEM] Module Preload Hints (`tools/gen-modulepreload.js`):** `main.js` reaches 121 further modules at a graph depth of 5, so without hints the browser can only discover each level after parsing the one above it — five sequential round-trip generations spent merely finding out what to fetch, with HTTP/1.1's six-connection limit making the tail of each generation block the start of the next. All 121 are now declared `rel="modulepreload"`, placed *after* the `r160.js` tag so three.js keeps first claim on the connection pool (nothing can run without the global `THREE` it defines). The list is generated from the real import graph rather than maintained by hand, breadth-first so emission order matches load order, and rewritten in place between markers — run `node tools/gen-modulepreload.js` after changing any import.
- **[UI] The Initial Chunk Build Has A Phase:** The single largest stretch of boot — **3678ms of 6131ms, sixty percent** — ran inside a bare `await` in `main.js` with no reporting whatsoever, freezing the bar mid-sweep for seconds. This was missed in the original audit and only surfaced once end-to-end phase timing existed to expose the gap. It now owns the `CHUNKS` phase and reports against `genStats.count` versus the chunk count the streamer is targeting, with flavour lines firing every 1.8s so long stalls on slower hardware still read as alive.

### Known

- **[ASSETS] MAINTENANCE and ARCHIVE will stay on the generator permanently, by design.** Both return assets a texture pipeline cannot represent: `leakStainGeo`, `paperGeo` and `coffeeStainGeo` are `BufferGeometry`; `bookMatSets` is an array of arrays that shares `pageMat` by reference across every entry, so exporting it would silently de-alias into distinct copies; and `archiveWallMat.userData` points at its own baseboard and trim materials, a cross-reference with nowhere to live in a flat metadata file. The exporter reports each of these by name and reason on every run rather than skipping them quietly. Both sectors generate in 1-2ms, so nothing meaningful is lost.
- **[PERFORMANCE] The in-world stutter fix is verified structurally, not by frame timing.** The blocking-drain path was confirmed by reading it, and the change is verified not to have broken boot, but **no traversal frame-time measurement was taken before or after** — the session's verification was all boot-side. The v1.1.3 notes are explicit that run-to-run variance on this class of stall can be as large as the effect being measured, so this should be treated as an understood mechanism corrected, not a demonstrated improvement, until a fixed-seed fixed-traversal benchmark runs against it.
- **[PERFORMANCE] The chunk phase is one chunk, not nine.** Final measurement: 9 chunks, 3823ms total, **worst single chunk 2186ms** — 57% of the phase in one build. Nearest-first queue ordering and predictive prefetch, both previously identified as candidates, address distribution rather than this. The unyielded quadratic breach-wall pass in `_buildChunkInterior` (allocating `[...chunkGroup.children, ...stagingMeshes]` per breach wall, `Array.includes` inside its inner loop, and a `fixtureData.filter` that reclones a bounding box per fixture per wall, with no yield anywhere in the block) is the more likely candidate. Instrument inside `_buildChunkInterior` before touching any of it.
- **[ASSETS] The off-thread decode depends on `imageOrientation: 'flipY'` being honoured.** Verified true on this browser. Any environment that accepts the option but ignores it would render every static texture vertically mirrored while reporting no error — the `THREE.TextureLoader` fallback only engages when `createImageBitmap` is absent or throws, not when it silently misbehaves. The mirror-match assertion used to verify this is worth keeping as a startup check if the engine ever ships to a wider browser matrix.
- **[SYSTEM] `_programKeepAlive` still grows without bound.** `warmMaterialVariants` pushes three material clones per warmed material and never releases them, while `_forgetMaterialPrograms` removes the entry from `_warmedMaterials` on chunk disposal — so a re-created identical material is re-warmed and adds three *more* clones. Unrelated to this pass's changes and unchanged by them, but it accumulates across a long traversal session.

## [v1.1.7] - 2026-08-17

_A Fixed Light Budget, and Arches That Curve_

### Fixed

- **[GRAPHICS] Arch Curves Read As Flat Segments:** The curved span of every archway showed its tessellation, each segment catching the light at its own angle and reading as if the arch were casting shadows onto itself. Nothing was occluding anything — `THREE.ExtrudeGeometry` returns a non-indexed mesh, so the `computeVertexNormals()` call in `StructureKit.buildArchCutout` gave every wall quad along the intrados a single flat face normal, and the curve was genuinely flat-shaded. Vertices lying on the arc's swept cylinder now take the analytic radial normal instead, with the sign lifted from the flat normal so the fix stays independent of the shape's winding; adjacent quads agree at their shared edge and the curve shades as one surface. Everything else — the extrusion caps, the outer box faces, the jambs — keeps its flat normal, so the hard edges stay hard. `curveSegments` also raised 24 → 32 for the silhouette. Both `CURVED ARCHWAY` and the Arch Halls share the builder and get it. Verified against the live geometry (`archCutout_1.2_0.8_1.5_4_1.5`): all 378 intrados vertices exactly radial, max deviation from the analytic normal `2.9e-8`, all facing inward consistently, and 63 distinct normals across the arc — one per ring position, shared between neighbouring quads, which is the smooth-shading signature.
- **[PERFORMANCE] Hard Stutter On Picking Up Tapes, Laptops and Notes:** Reported as a hitch on first reading a note or picking up a tape recorder, most noticeable on tapes and laptops. Not the document overlay, the story fragment or the foley — measured at 0.5ms for the entire `somatic-read` handler. Every one of these props carried its own `PointLight` child for its glow, and `WebGLRenderer.projectObject` returns early on any object whose `visible` is false, so `hit.visible = false` on pickup hid the light along with the mesh. That drops `NUM_POINT_LIGHTS`, which is part of every material's shader program cache key, and the entire visible set has to relink synchronously in the frame the player pressed E. Measured against a live 40-point-light scene: **26 new programs and ~72ms of compile for a single light appearing or disappearing**. It only bit the first time a given light count came up, since the programs are cached afterwards — which is why it read as "the first tape, the first laptop" rather than every pickup. Fixed structurally by pooling the glows (below); the pickup path now calls `releasePropLighting()`, which retires the prop's fixture and — belt and braces for anything that still parents a real light to a prop — re-homes stray lights onto the chunk group at zero intensity rather than letting them wink out with the mesh. Re-measured through the real placement path: **0 new programs on pickup, 0.1ms to release**.

### Changed

- **[GRAPHICS] Prop Glows Run Through The Light Pool (`PropGlow.js`):** Notes, documents, tapes, laptops, clipboards and the Atrium vending fronts each used to own a dedicated `PointLight`, which put a variable number of real lights in the scene — one per prop in every loaded chunk. Because the light count is baked into every material's program key, chunk loads, chunk unloads and pickups all moved it, and each move cost the relink measured above. A prop glow is now a LumenGrid fixture competing for the same fixed pool of 32 lights as every ceiling panel and lamp in the level, via `attachPropGlow()`; nine light sites were converted across `NarrativeProps.js` (six), `AnnexSector.js` (two) and `AtriumSector.js` (one), with per-prop colour, intensity, reach and local offset kept in one `PROP_GLOW` table so each reads exactly as it did. Three hooks were added to `LumenGrid` to carry them: `fixture.color`, since pool lights are recycled between fixtures and a prop glow has no emissive material to read its colour from; `fixture.slotBias` (+40 in distance-squared units, roughly "pretend it is 6m further away"), because a glow centimetres from the player's face would otherwise evict every room fixture around it and leave the room dark behind the note; and `fixture.noGlare`, because glare and pupil adaptation track the nearest fixture and these glows were not fixtures at all before. Fixtures declare the intensity they want the pooled light to end at, so `attachPropGlow` divides out the pool's 0.35 non-shadow slot scalar. Verified by placing a note through the real `placeEphemera` path: the pooled light comes out white at intensity **1.201** against the old raw light's 1.2, distance 3.0, position identical, landing in slot 6 — the first non-shadow slot, so a note on the floor never takes a shadow map. Scene census afterwards: 32 pooled point and 32 pooled spot lights, and **zero lights owned by a streamed chunk**. The 8 point and 3 spot lights outside the pool are the entity and player rigs, built once at boot; each entity's `deactivate()` leaves its group visible, so those never move the count either. The engine's light count is now constant for the whole session.
- **[UI] Virtual Cursor Hit-Testing Deferred To A Frame:** `SomaticInput._onMouseMove` hid `#virtual-cursor`, called `elementFromPoint`, then restored it — two synchronous style and layout flushes per event, and under pointer lock a high-polling mouse delivers mousemove far faster than the frame rate, so a document left open was paying hundreds of forced layouts a second. The hide was redundant to begin with: the cursor is already `pointer-events: none` and was never the hit result. The hover probe now runs once per frame through `_queueHoverProbe()` and clears its stale `.virtual-hover` when reading ends. The gamepad cursor path shares it.

### Known

- **[GRAPHICS] Prop glows now compete for the 32 pool slots, and can lose.** Two intentional consequences of pooling: a glow fades in over the pool's standard 0.6s spawn ramp as it comes into range rather than being lit the instant its chunk loads, and where 32 lights are already claimed nearby a distant prop may not light until the player approaches. `PROP_GLOW_SLOT_BIAS` in `PropGlow.js` is the knob if the balance reads wrong in play.
- **[GRAPHICS] This pass was verified against the live scene graph and renderer, not on screen.** Program counts, compile costs, the arch's normals and the resulting pooled light were all read off the running engine, which exercises the real code paths, but the preview surface could not composite frames during the session so nothing here has been eyeballed in motion.

_Closing the Warmup Gap and Watching the Boot Sequence Work_

### Fixed

- **[WORLD] Missing Crawlspace Covers:** Fixed a logic error inside `StructureKit.js` where overlapping cell placements were mistakenly deleting existing grates instead of preserving them. Crawlspaces and vents will no longer sporadically spawn missing their hatches and covers.
- **[SYSTEM] Boot Sequence Crash on `helpers.boxGeo`:** Fixed a runtime `TypeError` halting the `ChunkManager` during boot. A minor local scope issue while implementing the new doorknobs caused `addGrate` to check `helpers.boxGeo` instead of the instantiated `this.boxGeo`. Blueprints generating anomalous and hinged doors were also updated to properly reference the doorknob builder from their local context to prevent silently failing to attach handles.
- **[PERFORMANCE] Shader Warmup Missed Every Per-Sector Material:** A player reported the engine freezing for a couple of seconds while reading a laptop note in the Annex — right before the note itself appeared, matching the synchronous shader-compile stall class from v1.1.3 rather than anything specific to reading documents. Root cause: `ProceduralTextureFactory.lazyLoadSectorAssets()` — the call that builds the Annex/Impound/Boardroom/Atrium/Maintenance/Archive/Checkpoint/Incinerator texture bundles — fired in the background *after* `ShaderWarmup.run()` had already finished, so none of those eight sectors' materials existed yet when `ShaderWarmup._collectMaterials()` took its snapshot. Every one of them was guaranteed a cold, synchronous `renderer.compile()` the first time a player actually reached that sector. A second, narrower gap survived even for sectors whose base bundle *was* warmed: materials created lazily inside a blueprint's own `build()` callback — gated behind spawn rolls, keypad odds, furniture selection, e.g. the Annex laptop's screen-glow material or its exit key's metal — were invisible to `ShaderWarmup._materialiseLazySectorAssets`, which only ever calls a sector's top-level factory, never the `build()` function it returns. `Environment.setup()` now awaits `lazyLoadSectorAssets()` before `ShaderWarmup.run()`, and a new `LazyMaterialWarmup.js` replays roughly 39 of these `build()`-embedded singleton materials verbatim (copied, not retyped, to avoid transcription drift on colors/params) so every one of them compiles at boot instead of on first sight. Verified live: a fresh boot now warms 198 distinct materials across 25 batches, and warping through Archive, Clinic, Incinerator, Maintenance and Annex afterward produced zero frames over 30ms.

### Changed

- **[AESTHETICS] Segmented Fluorescent Light Ballasts:** Replaced the continuous, flat glowing ceiling seams in the Arch Halls with properly spaced, segmented fluorescent light ballasts. These feature distinct metal housings and 14-louver grilles to break up the geometry and read as realistic lighting fixtures.
- **[GAMEPLAY] Navigational Adjustments:** Raised the base speed of crawling from `15.0` to `22.0` and crouching from `30.0` to `40.0` inside `PlayerController.js` to make navigating tighter vents and spaces less sluggish. Dialed down the anomaly's volume multiplier from `0.4` to `0.15` in `Mixer.js` to ease audio fatigue while maintaining its presence.
- **[GRAPHICS] FXAA Off By Default:** Diagnosed from a separate player report — an RTX 4070 owner said dropping Internal Resolution to 50% "sped things up immensely." That setting resizes the live WebGL drawing buffer with no reload required, so an instant improvement from a resolution change can't be a shader-compile stall; it has to be fill-rate. `RenderEngine`'s post-processing stack runs up to two full-screen fragment passes every frame — FXAA's 8-sample blur, then the CRT/chromatic-aberration/vignette pass at up to ~14 samples in its glare-blur branch — and both scale with whatever the internal resolution is set to. FXAA now defaults off (`RenderEngine.getSavedFXAA()`, the `#fxaaToggle` checkbox, and `SaveManager.loadState()`'s checkbox sync all updated together); anyone who had already explicitly opted in (`state.fxaa === true`) keeps that choice.

### Added

- **[AESTHETICS] 3D Brass Doorknobs:** Added detailed 3D brass doorknobs to all crawlspace hatches and residential hinged doors throughout the facility. Procedurally generated `doorMat` textures are now injected during boot to overwrite the cached static textures, scrubbing the old baked-in 2D handles so they don't awkwardly sit underneath the new 3D meshes. The new doorknobs were also scaled up and repositioned toward the jamb to sit at a natural height and distance.
- **[UI] Continue / New Game Prompt on Boot:** Added `ContinuePrompt.js`, which now runs as the literal first statement in `main.js` — before `bootCtrl.init()`, before `RenderEngine`, before anything touches the network or IndexedDB. If a save exists in `localStorage` it blocks on a new full-screen choice (`#continue-check-overlay`, styled to match the existing boot terminal) instead of silently resuming: **Continue Session** proceeds exactly as before, **Purge & Start New Game** calls a new `SaveManager.purgeAllStorage()` — localStorage, sessionStorage, the IndexedDB texture cache, service worker unregistration, and the Cache API — then lets boot continue in the same page load rather than reloading, since nothing has been fetched yet to invalidate. The existing "Purge Memory" settings-menu button now calls the same static method instead of duplicating the clear logic. No save on record: the prompt never appears and boot is byte-for-byte unchanged. Verified live for all three paths — no save skips straight to boot, Continue restores the exact saved position and seed, New Game produces a fresh seed, a fresh spawn point, and confirmed-empty IndexedDB stores.
- **[TOOLING] Boot Sequence Logs Everything to Console:** The on-screen boot log only ever kept its last 4 lines, and nothing reached the console, so a player's "boot took 3-4 minutes" report was undiagnosable from a screenshot. `BootController` now mirrors every phase and progress message to `console.log`, tracks a duration per phase, and prints a `console.table` summary plus the complete log at the moment the threshold opens. Added `BootController.logDeviceInfo()`, logged once the renderer exists: GPU vendor/renderer string via `WEBGL_debug_renderer_info`, CPU core count, device memory, screen/viewport size, tab focus/visibility state, and the exact resolution/shadow/AA/FXAA/post-processing settings that boot is running with. The previously-silent `lazyLoadSectorAssets` and `ShaderWarmup._warm` steps now report per-bundle and per-batch timing, and any warmup batch over 200ms logs the specific material types that were in it. Verified live: a full phase-by-phase trail with a correct GPU read (`ANGLE (AMD, AMD Radeon RX 7800 XT...)`) on this machine.

### Known

- **[PERFORMANCE] `ChunkManager.drainShadowPrewarm` can still stall on its first bite.** Its per-frame 2ms budget is only checked *between* materials — the loop always compiles at least one full material before checking, and it force-enables `shadowMap.enabled` temporarily to do it regardless of the player's Shadow Quality setting. A single expensive shadow-program compile can blow past the budget arbitrarily. Likely explanation for a separate report of periodic hard stutters during play; not yet fixed here.
- **[PERFORMANCE] `clinicCurtainMat`'s warmed-cache entry goes stale almost immediately after boot** — its `.version` was observed climbing from 0 to 6 with no `.needsUpdate` call anywhere in the codebase touching it. Something outside the files reviewed this pass is bumping it, which would defeat re-use of the boot-time compile the next time a clinic curtain is built. Independent of the warmup-gap fix above (it would have defeated the original in-game lazy-warm just as easily); not yet root-caused.

## [v1.1.4] - 2026-08-16

_Immutable Checkpoints and Surviving the Initial Drop_

### Fixed

- **[SYSTEM] Falling Through Geometry on Reload:** When reloading from a save state deep inside the facility, players were occasionally dying instantly or teleporting into the middle of uninitialized sectors. The root cause was that gravity was still affecting the player during the initial 2-second boot window while the `ChunkManager` was asynchronously compiling the floor collision boxes. The player would fall out of bounds (`y < -15`), triggering a blackout death that randomized their location and forced the `needsSafeSpawn` routine to aggressively teleport them. The `Environment.isSpawning` state is now passed up to `main.js` to strictly enforce `player.isFrozen` until the initial geography has completely materialized. Additionally, `needsSafeSpawn` is now explicitly bypassed when successfully loading an existing save.
- **[SYSTEM] Incomplete Save Payloads:** `SaveManager.saveState` and the bootstrap logic in `main.js` were missing several key player parameters. Save states now successfully capture and perfectly restore stance (`isCrouching`, `isCrawling`), flashlight toggle status (`flashlightActive`), and the missing `hasExitKey` boolean inside the inventory.

## [v1.1.3] - 2026-08-16

_Warming the Shader Programs the Engine Actually Uses_

### Added

- **[GRAPHICS] Shadow-Depth Program Prewarm (`ChunkManager.queueShadowPrewarm`, `drainShadowPrewarm`):** Added a queue of materials awaiting their shadow-pass programs, fed from `warmMaterialVariants` so it catches both the boot warmup and every chunk build, and drained two ways. A 2ms slice runs in `animate()` immediately after `engine.render()`, so a compile lands in the gap between frames rather than inside one. A second bounded drain runs at the tail of `_buildChunkInterior`, where the chunk's own new materials can be retired while the chunk is still invisible and the build is already yielding — a chunk can otherwise mint a material and put it on screen in the same frame, outrunning the per-frame drain. The probe rig renders the **real** materials rather than clones, against two throwaway shadow-casting lights into a 4x4 target: a depth program's cache key does not depend on light counts, so two scratch lights produce exactly the programs the real fixtures need, and borrowing the live fixtures would leave their shadow maps holding a picture of a probe plane. Using originals also keeps this path out of `_programKeepAlive`. Three probe meshes per material — plain, instanced, and instanced with `instanceColor` set, since `instancingColor` is in the depth program's cache key as well as the visible one.

### Changed

- **[GRAPHICS] Shader Link Draining No Longer Blocks:** `_drainProgramLinks` called `getUniforms()` on every program after every warmup batch. That call blocks until the driver finishes linking, which is the point of it — but waiting per batch meant the driver linked eight materials while nothing else was submitted, serialising `KHR_parallel_shader_compile` into exactly the sequential link the r160 upgrade was made to escape, and re-waiting on programs already drained dozens of times over. Measured at boot: **7,126ms in the drain against 89ms of actual `renderer.compile()`**. Each program is now drained once, tracked in a `WeakSet`, and the boot warmup queues every compile without waiting on any of them. Completed links are collected a slice per frame in `animate()`, using the extension's `COMPLETION_STATUS_KHR` to skip programs still in flight rather than stalling on them. The material warmup phase fell from **7,270ms to 125ms**.

### Fixed

- **[GRAPHICS] Multi-Second Stalls On Reaching New Geography:** Streaming into unseen chunks could stop the engine dead for seconds at a time. Measured on a live build, a traversal requiring 26 new shader programs cost a single **2,764ms frame**, while one requiring 2 had a worst frame of 28ms — the stall is synchronous shader compilation, and it scales with how many programs are new. The cause is a structural gap in the warmup rather than a shortfall in it. Three builds a material's `depth` and `distanceRGBA` programs inside the shadow pass, and `_scopedCompile` calls `renderer.compile()`, which never runs that pass; `warmMaterialVariants` compounds it by never setting `castShadow` on its probes. Neither fix works alone. So the boot warmup spent roughly ten seconds compiling 558 material clones into 107 programs, of which only 11 were shadow programs, and the remaining 43 shadow programs compiled during play — one stall per corner turned. Counted directly off the renderer: **107 programs after boot against 209 after a few minutes of walking**, with 80% of the shadow programs arriving during gameplay. With the prewarm in place, shadow programs stand at 48 to 60 before the player moves, and across six aggressive traversals the **worst frame fell from 2,764ms to 231ms**, four of the six running clean at a steady 60fps with a worst frame of 23 to 30ms. Boot is unchanged at 14.8s, since the queue is drained during play rather than folded into startup.
- **[GRAPHICS] Renderer State Restored Around Probe Renders:** The prewarm renders into its own target with its own lights and its own scene graph. Render target, `shadowMap.enabled` and `shadowMap.autoUpdate` are captured and restored in a `finally`, and `scene.children` is swapped back regardless of outcome, so a failed probe cannot leave the renderer pointed at a 4x4 buffer. Verified live after a full session: render target back to `null`, probe rig absent from the scene graph, all 14 shadow-casting lights intact.

### Known

- **[GRAPHICS] Stalls are reduced, not eliminated, and further tuning is now below the measurement noise floor.** On a purged ANGLE program cache (AMD Radeon RX 7800 XT) the worst frame across five traversals sits around **400 to 435ms**, against a **2,764ms** baseline, with 11 to 25 programs still compiling during play. Those two figures are a range rather than a result: repeated runs of *identical* code produced 13 late / 435ms and 25 late / 399ms, so run-to-run variance is as large as the differences between the candidate fixes. Three further attempts were made and none survived measurement — a UV-less probe geometry (no better than noise), warming each chunk's real geometry-material-instancing combinations before the chunk becomes visible (worse: 592ms, and the warming work became the stall), and splitting the probe render per variant (worse). All three were reverted. The remaining late programs each differ from an already-warmed one by a single boolean field in the program cache key, so the mechanism is understood and the coverage is incomplete. Closing them needs a repeatable benchmark first — fixed seed, fixed traversal, cold cache per trial, several trials — because at this point the instrument cannot resolve the effect.
- **[GRAPHICS] Compiled shaders cannot be cached by the engine.** WebGL exposes no program-binary API — `getProgramBinary` is undefined and no binary extension is offered — so there is no way to persist compiled programs to IndexedDB alongside the texture cache. The browser's own on-disk program cache is the only reuse across reloads, and it is already doing its job: a genuinely cold first boot runs about 20s against 14 to 15s on every boot after. `TextureCache` purging does not touch it.

- **[GRAPHICS] Boot is unchanged at roughly 15 seconds, and cutting 7.1s out of the warmup did not move it.** The blocking drain was never on the critical path — it overlapped the initial chunk build, so removing it finished the chunk phase around six seconds earlier and the first full `engine.render()` simply absorbed the difference, since the links it no longer had to wait for it now waits for itself. Measured end to end on the live build: **0.2s** material warmup, **8.7s** initial chunk building, **1.1s** `compileAsync`, **5.5s** in the first `engine.render()`, **1.2s** of boot-bar animation and fade. There is no waste left in that sequence to delete; it is real work that happens before the threshold opens. Making boot shorter now means opening the threshold before all of it is finished, which is a design decision about how much first-minute stutter is acceptable, not a defect to fix.

- **[GRAPHICS] Three frames over 100ms survive across 27 seconds of streaming.** These are materials created and shown inside the same build, where even the pre-visibility drain has no runway. The test that produces them teleports six chunks at a time; a walking player gives the builder considerably more warning.
- **[GRAPHICS] `_programKeepAlive` grows unbounded at three clones per unique material,** reaching 1,062 in a single session. It belongs to the original `warmMaterialVariants` path, not the shadow prewarm, and is memory rather than framerate.

## [v1.1.2] - 2026-08-16

_Partitions That Stop Instead of Vanishing, Cell Claims and Permeability, and Doors That Have to Earn Their Keep_

> **Existing saves will build a different world.** The doorway planner now rejects any run it cannot prove is private, which changes how many `random()` draws each chunk spends before moving on, and the terminus exit table shrank from eleven weighted entries to four. Both alter the chunk PRNG stream, so corridors, sectors and set pieces downstream of the first rejected door will land elsewhere. `package.json` and `assets/textures/metadata.json` still read `1.1.1`; bump them if you want `TextureCache.checkVersion()` to purge, though nothing in this release touches a texture.

### Added

- **[WORLD] Span Walls (`ctx.retractSpanWall`, `ctx.spanClearanceToCell`, `ctx.spanClearanceToBox`, `ctx.retireStagedMesh`):** Added a description a partition can carry of its own growth — a fixed anchor, a unit direction and a length — declared in the blueprint's local space and lifted into world space once the group transform is baked in. A wall carrying `userData.wallSpan` can be shortened in place: geometry re-requested from the `buildWall` cache at the new width, position re-anchored so the fixed end does not move, collision box re-indexed, and baseboard parts rescaled along the same local axis. Lengths quantise by `Math.floor` rather than `Math.round`, because rounding up pushes the retracted end back into the obstruction it was retracting away from. `spanClearanceToCell` returns the span's own length when the cell is out of its way, so a clip is only ever a shortening and never an extension.
- **[WORLD] Cell Claims (`ctx.claimCell`, `ctx.isCellClaimed`):** Added a per-chunk registry of cells a structure has taken. Duct and crawlspace networks carve several cells each but only ever mark their *origin* cell forced, so any pre-pass scanning for obstacles sees empty grid where a duct hub actually stands. Claims are read after the whole chunk is staged, which is the only point in the build where the answer does not depend on which structure was rolled first. `DuctOrVent`, `CrawlspaceDuct` and `CrawlspaceHall` claim alongside the `markOccupied` and `setWall` calls they were already making.
- **[WORLD] Permeable Cells (`ctx.markPermeable`, `ctx.isCellPermeable`):** Added the mirror of a claim. `RANDOM PILLAR` and `ROUND PILLAR` hold a wall cell without filling it, so a partition running through one reads as deliberate rather than broken. Permeability only ever softens the wall-cell test in a blueprint's outward scan; it is never consulted against a claim or a forced blocker, so a pillar cannot talk a duct into being passable.
- **[PHYSICS] `SpatialHashGrid.remove(box)` — Targeted Collider Removal:** Colliders previously left the grid only when an entire chunk unloaded. Added a per-box unindex, with `removeByChunk` refactored to share the same `_unindex` walk so the two paths cannot drift.

### Changed

- **[WORLD] A Doorway Run Must Earn Its Door:** `_planDoorwayRun` now asks, after planning and before committing: once this plan is applied, does any carved cell touch open floor that is not part of the run? If it does, the plan is discarded and the wall cell falls through to a normal structure roll. The seal ring was always meant to guarantee privacy but declined to touch four kinds of cell — anything outside the chunk, another run's reserved cells, artery cells and airlock aprons — and each decline was a mouth the player could walk around to. One question replaces four special cases, and the out-of-chunk case is genuinely unfixable rather than merely unhandled, since `setWallFn` writes into a chunk-scoped grid that the neighbouring chunk never sees. Measured across 400 chunks: **doors fell from 6.21 to 4.21 per chunk** (worst case 12 to 9), and **chunks containing a run reachable without opening its door fell from 385 of 400 to 0 of 400**. `DOORWAY_RATE` is untouched at `0.08`; the count came down because plans stopped qualifying, not because a number was turned down. For reference if it needs turning down anyway: `0.05` gives 3.13 doors per chunk with 11 of 400 chunks doorless, `0.03` gives 2.07 with 50 of 400 doorless.
- **[WORLD] Doorway Termini Keep Their Cell Solid:** The terminus exit table held `CRAWLSPACE_HALL` ×3, `empty_door_frame` and `CREVICE_HALL` — five of eleven weights — all of which set the terminus cell **non-wall**. Since the seal ring only ever covered cells adjacent to the corridor, opening the terminus reconnected the run to open level exactly one cell further out, and `apply()` had already sealed that same cell moments earlier only for the terminus loop to overwrite it. The table is now ducts only, so a run ends in a wall you crawl through rather than an opening reachable from the far side. The `setWallFn` call that used to test the name for openness is now an unconditional `true`, since the tautology hid the intent.
- **[WORLD] Private Runs Are Built From the Chunk's Hall Vocabulary:** Corridor cells cleared their forced structure to `null` and read as bare hallway, which is what made these feel like filler even in the cases where they *were* private. They now take the chunk's `pathTheme`. No new machinery was needed: `ARCH_HALL` and `RIDE_QUEUE_HALL` already read their own neighbours to decide whether they are a straight, a turn or a dead end, so they stamp onto a doorway run exactly as they do onto an artery. **Carved cells carrying hall architecture rose from 262 of 6575 to 3399 of 4086** — 4% to 83%.
- **[WORLD] Partitions Thread Pillars:** The outward scan in `EmptyDoorFrame` broke on any wall cell, so a partition stopped one cell short of every pillar. It now counts past a permeable cell and keeps going until it meets a cell that is genuinely solid, threading the pillar and landing on real wall. Dividers built in *empty* cells were always passed through — there was no signal to stop on — so this brings the two cases into agreement rather than introducing a new behaviour.

### Fixed

- **[WORLD] Partition Walls Deleting Themselves and Leaving Their Baseboard Behind:** Two defects wearing one costume. `ctx.setWall(cell, false)` spliced any staged mesh whose **centre** landed within half a cell of the cleared cell, and a partition extending one cell past a door frame has its centre at −3.35 against a 4-unit cell — inside the neighbour. A duct network carving that cell took the whole wall, jamb to tip. Meanwhile `addGeometry` built the baseboard body and trim as orphan siblings marked `noCollision`, and every cleanup path in the engine `continue`s past anything `noCollision`, so the trim was structurally immune to every removal its wall was subject to. Walls now retract to the cell face instead of being spliced, and baseboards carry an owner link so they are resized and retired with their parent. Verified across both rotations, both growth directions and three extension lengths: retracted ends land flush on the obstructing cell's near face in every case.
- **[WORLD] Partitions Growing Through Duct Networks:** Retraction only worked in one time direction. `setWall` shortens a partition that already exists, but a duct carved *before* the frame was rolled had already finished, leaving nothing to retract and only the build-time blocker scan standing in the way — and that scan reads `getForcedStructure`, which is set on a network's origin cell and nowhere else. `CRAWLSPACE_DUCT` was in the blocker list and was duly respected at its origin while the partition walked straight through the rest of the network. Fixed by clipping against **claimed cells** in the post-staging breach pass, which is ordering-independent by construction. Duct-then-frame and frame-then-duct now produce identical geometry, and the clip is idempotent — running it twice does not shave the wall further.
- **[WORLD] Rotated Door Frames Reporting the Wrong Growth Axis:** `Math.cos(Math.PI / 2)` is `6.123e-17`, not zero. Left unsnapped, a rotated door frame reported a growth direction with a sliver on both axes and every downstream axis test picked the wrong one. Direction components are snapped below `1e-9`, and the axis test asks `Math.abs(dirX) > 0.5` rather than `!== 0`, so the two guards are independent.
- **[WORLD] Partitions Retracting to Nothing From the Wrong Side:** `spanClearanceToCell` returned a negative distance for cells sitting *behind* a partition's anchor, which read as "retract to zero" — so clearing a cell to the right of a door frame deleted the partition growing to the left, which is the original bug in a new coat. A negative result now distinguishes the two cases it was conflating: a cell that swallows the anchor leaves the wall nothing to stand on and returns zero, a cell merely on the far side of the frame is none of that wall's business and returns the span's full length. Caught by a sweep over both rotations and all four growth directions, which flagged 36 cases against a single root cause.
- **[PHYSICS] Invisible Collision Where a Wall Was Removed:** `addGeometry` inserts an AABB into the spatial grid, and the old splice removed the mesh from the staging list without touching it. Every partition deleted by `setWall` left a full-height collider standing in an empty corridor. Removal and retraction now both re-index, using the mesh's own stored `collisionBox`.

### Removed

- **[WORLD] Dead Doorway Planner in `ChunkManager`:** Deleted 195 lines — `_planDoorways` and `_planDoorwayRun`, whose only callers were each other. The live planner has lived in `ChunkWorker.js` since doorway planning moved into the worker, and the `ChunkManager` copy was a strictly older snapshot: no `pathTheme` or artery exclusion, no forced-structure lookup, no privacy test, and the pre-fix exit table still in place. Nothing in it was worth salvaging, and leaving it meant the next person to change doorway behaviour had even odds of editing the copy the engine never executes. `_isAirlockApron` and `_airlockApron` are untouched and still live.

## [v1.1.1] - 2026-08-15

_Deterministic Sector Placement, Single-Mesh Archways, and Non-Overlapping Floor Clutter_

> **Existing saves will build a different world.** Sector identity is now derived from the seed rather than read back from savedata, the macro-placement hash was replaced, and two `random()` draws were removed from every chunk's stream. `TextureCache.checkVersion()` purges textures on a version change but deliberately leaves save state alone, so nothing clears the old geography for you. Clear your save rather than wonder why a corridor moved.

### Added

- **[WORLD] `SectorPlacement.js` — Placement as Arithmetic:** Added `src/world/SectorPlacement.js`, which answers "is this chunk a sector, and which one" as a pure function of `baseSeed` and the chunk coordinates. Nothing in it reads the player, the save file, or which chunks happen to have been built already. `isMacroChunk` resolves the minimum-spacing rule by greedy claiming ordered by ring distance then hash, recursing upward through strictly higher ranks and memoising, so the claim order is defined by coordinates instead of by the player's route. `sectorIdFor` shuffles a deck per 12-chunk region and deals by position within it, preserving the old bag's no-repeats character: median 11 sectors per region against a pool of 11, with 1 region in 377 across five seeds containing an early repeat. `isExitChunk` fixes the exit from the seed, spiralling outward if the nominated region holds no sectors. Memo maps are bounded at 200k entries and are pure derived values, so dropping them costs a recompute and nothing else.
- **[WORLD] `ctx.addBaseboardBox`:** Added an explicit baseboard emitter. `buildWall` tags its meshes with `baseboardFootprint` and `addGeometry` trims them automatically, but that path takes one solid box per mesh. Geometry with a doorway punched through it has to place its trim per jamb.

### Changed

- **[WORLD] Curved Archways Are One Extrusion:** `buildArchCutout` now takes a `springY` and extrudes the jambs and the vault as a single outline from the floor up, replacing the previous arrangement of two `buildWall` pillars with an arch dropped on top. The fifth argument was already `verticalClearance`, so only its meaning changed at the call site. Its UV pass was rewritten around a single arc-length variable that unrolls the whole interior as one strip — up the right jamb, over the crown, down the left jamb — while the outer shell and ceiling keep the flat `y/3` mapping that tiles with neighbouring wall blocks. `addArchCutoutColliders` gained the same parameter and its jamb boxes now run floor to ceiling in one pass. Three draw calls became one.
- **[WORLD] Sector Assignment No Longer Uses a Bag:** `env._sectorBags` is deleted. `discoveredSectors` and `_macroChunkHashes` are still written and still saved, so `Compass` and `SaveManager` are untouched, but nothing reads them to decide what a chunk is. The exit-phase pool swap is also gone: there is one pool now, and the seed-fixed exit chunk wears its dealt sector until the player is actually holding the key. **Checkpoints now spawn during the exit phase, which is the intended behaviour.** Their suppression was never a design decision — it fell out of `poolKey` filtering `s.id !== "CHECKPOINT"` to make room for `EXIT` in a fixed-size bag, so the endgame quietly lost its checkpoints as a side effect of how the bag was refilled. Removing the swap fixes that and removes a determinism break in the same stroke.
- **[WORLD] Macro Density 10.2% → 9.2%, and the Old Number Was Inflated:** The previous candidate roll was `baseSeed + x*104729 + z*1299827`, one LCG step, second draw. Measured across 9,409 chunks it is close enough to linear in the coordinates that two diagonally adjacent chunks could **never** both be candidates — zero occurrences — and an adjacent chunk in x was a candidate only 21.45% of the time against a 40.05% base rate. That anti-correlation spaced candidates out for free, which is the only reason greedy claiming packed them to 10.2%. Sector placement had a lattice baked into it. With a uniform hash the honest packing is 9.2% at the current `macroThreshold` of 0.15; set it to 0 for 9.6%, raise it to thin sectors out.
- **[WORLD] Book Piles Are Placed on a Ring:** `spawnClutter` drew each pile position independently from the same small box. Piles are now laid on a ring sized so neighbours clear each other at every rotation, with the two draws that used to pick a raw position spent instead on radial and tangential wobble inside whatever slack remains. Same number of `random()` calls in the same order, so this change alone does not disturb seeds. The ring's phase derives from the room's own coordinates so the piles do not read as one clock face stamped across every room. `STACK_RADIUS` derives from the book dimensions rather than sitting as a magic number; brute-forced against every rotation and jitter corner it comes out to 0.3416, matching the constant exactly.

### Fixed

- **[GRAPHICS] The Seam Where Archways Spring:** Three separate misalignments stacked on one horizontal line at y=1.5, which is player eye height. First, `buildWall` inflates its box by 0.02 on every axis to hide cracks between grid walls, but the arch extrusion is exact — measured, the pillar top sat at 1.510 against an arch base of 1.500, its inner face at -1.190 against a springline of -1.200, and it overhung the arch depth by 0.01 at both mouths. That 0.01 ledge is a horizontal surface catching a different lighting angle than either wall around it. Second, `BoxGeometry` maps its ±X faces `u = (2 ∓ z)/4` while `buildArchCutout` mapped `u = z/4`, so the texture broke half a tile out of phase on the right jamb and half a tile out of phase *and* mirrored on the left. Third, and worst, the arc unrolls its V by arc length while the pillars mapped `v = y/3` from the floor, leaving the left springline meeting the arch at a **1.257-tile jump**; only the right jamb was ever continuous. Merging the geometry removes all three, because there is no longer a join to misalign. Verified: v runs 0.0000 at the right jamb floor to 2.2566 at the left, in uniform 0.1571 steps across the arc, against a predicted total unroll of 6.7699.
- **[WORLD] Books Interpenetrating on the Floor:** Two to four piles drew independently from a box 0.64 units wide on desks and 1.44 on floors, while a pile needs 0.683 between centres to stay clear at any rotation. Measured across 50,000 seeds, **98.9% of pile pairs overlapped on desks and 45.0% on floors**, closest pairs 0.0004 and 0.0010 apart. On a desk the box was narrower than the clearance, so two piles physically could not both fit; overlap was the design rather than an unlucky draw. Now zero overlapping pairs across 166,373 pairs at both call sites, closest pair 0.6832 and 0.8656, with piles staying within 3cm of the old footprint on desks and packing tighter than before on floors.
- **[WORLD] Sectors Changing Identity Across a Reboot:** Five independent breaks, any one of which was enough. (1) `env._sectorBags.pop()` made a chunk's sector a function of how many macro chunks were generated before it, so the answer moved with the player's route. (2) The bag reshuffled using the chunk-local PRNG of whichever chunk happened to drain it, handing that chunk's identity control of the next full cycle. (3) `poolKey` switched on `isExitPhase`, so a chunk first seen before the exit key drew from a different deck than the same chunk seen after. (4) `structuralShift`, derived from live `player.paranoia`, was added into the chunk PRNG seed — and paranoia is not in `saveState`, so it reset to zero on boot and every chunk built while the player was scared regenerated as a different chunk afterwards, including the `random() > 0.60` roll deciding whether it was a sector at all. Measured, this moved the seed for 1681 of 1681 chunks between calm and scared; it now moves for none. (5) The spacing check read `_macroChunkHashes`, which only holds chunks already built, so between two candidates within `macroMinSpacingChunks` the winner was decided by approach direction. Savedata was papering over all of this: `discoveredSectors` was the source of truth for world layout, `saveState` runs on a 2500ms `requestIdleCallback` interval, and `_sectorBags` was never persisted at all — so a reboot in the seconds after discovering a sector lost it, which is exactly the window this presented in. Verified across three sessions on one seed — a winding route, the same route reversed on a cold boot, and cold queries with no route at all — 83 sectors, 0 disagreements. Zero spacing violations across five seeds and 47,045 chunks.
- **[PHYSICS] Archway Colliders Rebased:** `addArchCutoutColliders` previously started its jamb boxes at the old springline, since the pillars below carried their own automatic bounding boxes. With the geometry merged the jambs are covered in one pass from y=0 to the ceiling. Verified 26 boxes spanning 0.000 to 3.000 with the walkway clear at head height.

## [v1.1.0] - 2026-08-15

_Spatial Duct Darkness, Honest Blueprint Probabilities, and Curved Surface Collision_

### Added

- **[LIGHTING] `DuctLighting.js` — Ambient Rejection for Sealed Spaces:** Added `src/core/DuctLighting.js`, which makes duct and crawlspace interiors reject the scene-wide ambient bounce while still taking the flashlight and any fixture spilling through an open grate at full strength. Implemented via a shared 1x1 black `aoMap` and an `aoMapIntensity` dial, because `aomap_fragment` multiplies `reflectedLight.indirectDiffuse` only — direct light lands in `directDiffuse` and is untouched. `DUCT_AMBIENT_FRACTION` is the single tuning knob (currently `0.02`; `0` gives pure black). Note that render layers cannot do this job in r160: lights are gathered once per scene render and filtered only against the camera, and ambient/hemisphere contributions reach the shader as global uniforms summed unconditionally for every fragment.
- **[WORLD] Cumulative Blueprint Selection (`StructuralBlueprints.select`):** Added a cumulative-sum selector plus a one-time validation warning if the probability table ever sums past 1.0. Returning `null` past the end of the table is the explicit signal to build a plain wall, so probabilities summing to less than 1.0 now mean "the rest of the time, nothing special happens here".
- **[WORLD] Single Plain-Wall Implementation (`ctx.buildDefaultWall`):** Added one helper that builds a featureless full-cell wall and tags it with `isDefaultWall`/`cellX`/`cellZ`. Both `ChunkManager`'s fallback and The Oasis's decline path now route through it, so every plain wall is addressable by cell and `ctx.setWall`'s metadata fast path can find it.
- **[WORLD] Curved Surface Colliders (`ctx.addCurvedCornerColliders`, `ctx.addArchCutoutColliders`):** Added staircase collider generators for the two pieces of cut-out geometry in the codebase. Boxes are built in the 2D shape space the geometry is defined from and pushed through the mesh's own world matrix; every orientation is a multiple of 90 degrees, so the transformed bounds stay exact and all rotations fall out without per-orientation cases to keep in sync.
- **[WORLD] Crawl-Height Awareness (`ctx.isLowClearance`):** Added a pre-pass-answerable predicate reporting whether an open cell has had its headroom dropped below standing height. Duct blueprints consult it when choosing exits, alongside the existing `isAirlockApron` refusal.

### Changed

- **[WORLD] `prob` Is Now an Actual Probability:** The structural matrix was previously sorted descending and selected with `find(roll >= s.prob)`, making a blueprint's real chance the *gap to the entry above it* rather than its own number. Nothing read that way, so all 24 values were authored as if they were probabilities and every one was wrong — The Observer declared `0.035` and fired at 6.5%, seven set pieces were squeezed into 0.1–0.45% bands, and The Oasis declared `0.00` yet took 2% of all cells. Values were rewritten to the windows each blueprint actually had, so the world was unchanged at the point of conversion; only the numbers became honest. The matrix is no longer sorted, since order no longer affects the odds.
- **[WORLD] Vertical Pipe Density Cut ~88x:** `PIPE CLUSTER` dropped from an effective 8% of wall cells to 1%, matching the archway rate. Each cluster still drops 2–4 pipes, so pipes remain marginally more common than archways as props while the feature itself is now equally rare. Measured across a fixed traversal: 1323 pipes before, 15 after. The freed budget was scaled proportionally across the other 22 blueprints (factor 1.076923), leaving the table summing to exactly 1.000000.
- **[WORLD] Curved Archway Five Times More Common:** `CURVED ARCHWAY` raised from 0.002 to 0.01 (roughly 1 in 500 wall cells to 1 in 100), with the difference taken from `BLOCKY OBSTRUCTION`. The blueprint was never broken — it was spawning at exactly its authored rate, which simply sat in the band where you would almost never meet one.
- **[WORLD] Crawlspace Duct Interior Linings Restored:** The thin interior linings were reinstated, rebuilt from a new `ductWallMat` rather than `sharedWallMat`. Every structural block in that blueprint is dual-faced — its inward face lines the duct, its outward face is corridor wall — and a material applies to a whole mesh, so darkening a block would punch a dark patch into the corridor around the grate. The linings are the only surfaces that are duct interior and nothing else, and they restore the anti-snag padding on the interior corners as a side effect.
- **[WORLD] The Oasis Declines Instead of Duplicating a Wall:** Only one oasis exists per chunk, so every roll after the first declines. It now returns `false` and hands the cell back to be filled with a plain wall, rather than building a second, untagged copy of one itself.

### Fixed

- **[GRAPHICS] Ducts and Crawlspaces Lit by Posture, Not Place:** Fixed the jarring fade to black on entering a duct. `AtmosphereManager` drove the global ambient light to zero whenever `player.isCrawling` was set, which got both halves wrong: it darkened the entire visible scene rather than the duct — including the lit room still visible through the open grate — and it fired on *entry* rather than on *sight*, so a duct read as fully lit until the player climbed in. Darkness is now a property of the geometry, so an opened grate reveals a dark interior immediately and climbing in changes nothing. Also removes the framerate-dependent, non-dt-scaled lerp that made the transition play at different speeds on different machines.
- **[PHYSICS] Walking Through Curved Alcove Walls:** The curved corner block is flagged `noCollision` (its automatic bounding box would be the whole cell) and was given two 0.3m slabs along the cell's outer edges — the collision hull of a *square* corner. Since the arc radius equals the cell size, the curve cuts up to 1.66m into the cell, leaving that entire volume walkable until the player hit the right angle hiding behind it. Replaced with a 32-step staircase sitting fractionally proud of the surface, so the player stops just before the wall and can never clip into it. Measured worst-case standoff 0.099m against a 0.40m player radius.
- **[PHYSICS] Curved Archways Forcing an Unnecessary Crouch:** The arch block's automatic bounding box spanned the whole opening from the springing line up, so a standing `physicalTop` of 2.5 was stopped dead by an arch showing 2.7m of clear headroom at the crown. Replaced with two exact boxes for the solid haunches plus a 24-step equal-angle staircase under the arc — equal-angle so the error collapses to nothing at the crown, where headroom decides whether the player fits, and concentrates at the springing where it merely fattens the pillar. Measured 2.609m of clearance at the centre against a predicted 2.70m; standing passage confirmed.
- **[WORLD] Crawlspace Halls Blocking Duct Doors:** A grate opening into a crawl-height corridor left nowhere to stand up and line up with the hatch. `pathTheme` themes an entire corridor run as `CRAWLSPACE_HALL` in a quarter of chunks and ducts take roughly a fifth of all wall cells, so the two met constantly. Ducts now refuse an exit into a low-clearance cell — answerable because the hall is assigned in the pre-pass, long before any duct rolls, so even a randomly placed duct sees it coming. Where the duct was itself placed deliberately as a corridor terminus, the hall yields instead and the cell reverts to full height, since refusing there would strip the duct's only exit and collapse the set piece into plain wall. Measured 1 of 363 grates still touching a crawl-height cell, down from effectively all of them; the remainder are chunk-boundary cases, as `forcedStructuresGrid` is per-chunk.
- **[WORLD] Duct Blueprint Exit Guards Missed the Network BFS:** The crawl-height and apron refusals on `CRAWLSPACE_DUCT` and `DUCT OR VENT` only covered the seed cell's four faces. Most of a network's exits are opened by the flood fill instead, which took a separate branch — the guard is now applied on both paths.
- **[WORLD] Self-Lit Duct Linings:** `ductMat` carries an emissive at 0.15 intensity, and emissive is added *after* the ambient occlusion term and is not occluded by it, so vent linings would have kept glowing inside an otherwise dark duct. `makeDuctInterior` now zeroes it.

